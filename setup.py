#!/usr/bin/env python
# -*- coding: utf-8 -*-

# This setup.py exists for backwards compatibility with tools that don't support pyproject.toml

import setuptools

if __name__ == "__main__":
    setuptools.setup()
